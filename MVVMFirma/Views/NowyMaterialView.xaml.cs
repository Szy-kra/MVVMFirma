using System.Windows.Controls;

namespace MVVMFirma.Views
{
    /// <summary>
    /// Logika interakcji dla klasy NowyMaterialView.xaml
    /// </summary>
    public partial class NowyMaterialView : UserControl
    {
        public NowyMaterialView()
        {
            // Jeśli to pole jest podkreślone na czerwono, zrób Rebuild.
            // Generuje się ono automatycznie z pliku XAML.
            InitializeComponent();
        }
    }
}