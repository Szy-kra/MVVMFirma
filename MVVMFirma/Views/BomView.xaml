<UserControl x:Class="MVVMFirma.Views.BomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">

    <Grid Background="#F0F0F0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#CCCCCC" BorderThickness="0,0,1,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Szukaj Indeksu:" FontWeight="Bold" Margin="0,0,0,5" Foreground="Black"/>
                <TextBox Grid.Row="1" Text="{Binding FrazaWyszukiwania, UpdateSourceTrigger=PropertyChanged}"
                         Height="25" VerticalContentAlignment="Center" Margin="0,0,0,10" Foreground="Black"/>

                <ListBox Grid.Row="2" ItemsSource="{Binding ListaIndeksow}" SelectedItem="{Binding WybranyIndeks}"
                         DisplayMemberPath="KodIndeksu" Foreground="Black"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="Podgląd struktury dla:" FontSize="14" Foreground="Black"/>
                <TextBlock Text="{Binding WybranyIndeks.KodIndeksu}" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding StrukturaBOM}" AutoGenerateColumns="False"
                      IsReadOnly="True" Background="White" Foreground="Black">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Składnik (Materiał)" Binding="{Binding Material}" Width="380"/>
                    <DataGridTextColumn Header="Ilość" Binding="{Binding Ilosc}" Width="100"/>
                    <DataGridTextColumn Header="Jedn." Binding="{Binding Jednostka}" Width="80"/>
                    <DataGridTextColumn Header="Udział %" Binding="{Binding Udzial}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Grid.Row="2" Content="Dodaj / Edytuj Strukturę (Nowy BOM)" 
                    Command="{Binding ShowNowyBomCommand}"
                    Width="300" Height="40" HorizontalAlignment="Right" Margin="0,15,0,0"
                    Background="#3498DB" Foreground="White" FontWeight="Bold"/>
        </Grid>
    </Grid>
</UserControl>