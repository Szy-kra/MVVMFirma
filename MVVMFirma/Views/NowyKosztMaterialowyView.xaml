<UserControl x:Class="MVVMFirma.Views.NowyKosztMaterialowyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="500">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <StackPanel Margin="25">
            <TextBlock Text="DODAWANIE KOSZTU MATERIAŁOWEGO" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

            <Label Content="Wybierz materiał z bazy (tabela Materialy):" FontWeight="Bold"/>
            <ComboBox ItemsSource="{Binding ListaMaterialow}" 
                      SelectedItem="{Binding WybranyMaterial, ValidatesOnDataErrors=True}"
                      DisplayMemberPath="KodMaterialu"
                      Height="30" Margin="0,5,0,15"/>

            <Label Content="Potwierdź kod (lub zmień opisowy):" FontWeight="Bold"/>
            <TextBox Text="{Binding KodMaterialu, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Content="Cena Jednostkowa (PLN):" FontWeight="Bold"/>
            <TextBox Text="{Binding CenaJednostkowa, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Content="Jednostka Miary (z listy):" FontWeight="Bold"/>
            <ComboBox ItemsSource="{Binding DostepneJednostki}" 
                      SelectedItem="{Binding JednostkaMiary}" 
                      Height="30" Margin="0,5,0,15"/>

            <Label Content="Dodatkowy Opis:" FontWeight="Bold"/>
            <TextBox Text="{Binding Opis}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

            <Label Content="Data operacji:" FontWeight="Bold"/>
            <DatePicker SelectedDate="{Binding DataModyfikacji}" Margin="0,5,0,20"/>

            <Button Content="ZAPISZ KOSZT W BAZIE" 
                    Command="{Binding SaveCommand}" 
                    Height="45" Background="#27AE60" Foreground="White" FontWeight="Bold"/>

            <ItemsControl ItemsSource="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=StackPanel}, Path=(Validation.ValidationAdornerSiteFor).InternalErrors}" Margin="0,15,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" Foreground="Red" FontStyle="Italic"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>