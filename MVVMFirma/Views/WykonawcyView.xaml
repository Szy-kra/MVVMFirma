<UserControl x:Class="MVVMFirma.Views.WykonawcyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid >


        <DataGrid ItemsSource="{Binding Wykonawcy}"
                  AutoGenerateColumns="False"
                  Background="White"
                  Foreground="Black"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  Margin="10"
                  RowHeight="35">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Nr wykonawcy"
                                    Binding="{Binding NrWykonawcy}"
                                    Width="120"/>

                <DataGridTextColumn Header="Nazwa wykonawcy"
                                    Binding="{Binding Nazwa}"
                                    Width="200"/>

                <DataGridTextColumn Header="NIP"
                                    Binding="{Binding NIP}"
                                    Width="120"/>

                <DataGridTextColumn Header="Telefon"
                                    Binding="{Binding Telefon}"
                                    Width="150"/>

                <DataGridTextColumn Header="Kraj"
                                    Binding="{Binding Kraj}"
                                    Width="120"/>

                <DataGridTextColumn Header="Miasto"
                                    Binding="{Binding Miasto}"
                                    Width="150"/>

                <DataGridTextColumn Header="Kod pocztowy"
                                    Binding="{Binding KodPocztowy}"
                                    Width="120"/>

                <DataGridTextColumn Header="Ulica"
                                    Binding="{Binding Ulica}"
                                    Width="150"/>

                <DataGridTextColumn Header="Nr domu"
                                    Binding="{Binding NrDomu}"
                                    Width="80"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>

