<UserControl x:Class="MVVMFirma.Views.WykonawcyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#34495E" Padding="10">
            <TextBlock Text="Zestawienie: Wykonawca - Adres - Zlecenie" 
                       Foreground="White" FontSize="18" FontWeight="Bold"/>
        </Border>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding ListaZbiorcza}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  Margin="5"
                  Background="White"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#DDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kod Wyk." Binding="{Binding NrWykonawcy}" Width="100"/>
                <DataGridTextColumn Header="Nazwa Firmy" Binding="{Binding Nazwa}" Width="150"/>
                <DataGridTextColumn Header="NIP" Binding="{Binding NIP}" Width="100"/>

                <DataGridTextColumn Header="Miasto" Binding="{Binding Miasto}" Width="100"/>
                <DataGridTextColumn Header="Ulica" Binding="{Binding Ulica}" Width="120"/>
                <DataGridTextColumn Header="Nr" Binding="{Binding NrDomu}" Width="50"/>

                <DataGridTextColumn Header="NR ZLECENIA" Binding="{Binding NrZlecenia}" Width="150" FontWeight="Bold" Foreground="Blue"/>
                <DataGridTextColumn Header="Data" Binding="{Binding DataZlecenia, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                <DataGridTextColumn Header="Ilość" Binding="{Binding Ilosc}" Width="80"/>
                <DataGridTextColumn Header="Uwagi" Binding="{Binding Uwagi}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>