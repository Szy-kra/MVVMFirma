<UserControl x:Class="MVVMFirma.Views.NoweZlecenieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="White" 
             d:DesignWidth="1000" d:DesignHeight="800">

    <UserControl.Resources>
        <ControlTemplate x:Key="ErrorTemplateWithText">
            <StackPanel Orientation="Horizontal">
                <AdornedElementPlaceholder Name="ControlWithError" />
                <TextBlock Text="{Binding ElementName=ControlWithError, Path=(Validation.Errors)[0].ErrorContent}" Foreground="Red" Margin="5,0" VerticalAlignment="Center"/>
            </StackPanel>
        </ControlTemplate>
        <Style x:Key="BaseTextBoxStyle" TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ErrorTemplateWithText}" />
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <Label Content="Wykonawca:" Margin="5,0"/>
                    <ComboBox Margin="5" ItemsSource="{Binding ListaWykonawcow}" SelectedValue="{Binding IdWykonawcy}" DisplayMemberPath="Nazwa" SelectedValuePath="Id"/>

                    <Label Content="Nr Zlecenia:" Margin="5,5,5,0"/>
                    <TextBox Margin="5" Style="{StaticResource BaseTextBoxStyle}" Tag="Wpisz numer..." Text="{Binding NrZlecenia, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

                    <Label Content="Indeks (Towar):" Margin="5,5,5,0"/>
                    <ComboBox Margin="5" ItemsSource="{Binding ListaIndeksow}" SelectedItem="{Binding WybranyIndeks}" DisplayMemberPath="KodIndeksu"/>

                    <Label Content="Ilość zlecona:" Margin="5,5,5,0"/>
                    <TextBox Margin="5" Style="{StaticResource BaseTextBoxStyle}" Tag="Ilość..." Text="{Binding Ilosc, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

                    <Button Content="ZAPISZ ZLECENIE" Command="{Binding SaveCommand}" Height="35" Margin="5,15,5,5" Background="#2196F3" Foreground="White" FontWeight="Bold"/>
                </StackPanel>

                <Border Grid.Column="1" BorderBrush="#333" BorderThickness="2" CornerRadius="5" Padding="20" Width="600" Height="200" HorizontalAlignment="Center" VerticalAlignment="Top">
                    <DockPanel>
                        <TextBlock Text="OPIS DETALU" DockPanel.Dock="Top" Foreground="Gray" FontWeight="Bold" FontSize="10" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding OpisDetalu}" FontWeight="Bold" FontSize="32" Foreground="Black" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </DockPanel>
                </Border>
            </Grid>

            <Border Grid.Row="1" Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1" Margin="0,20,0,10" Padding="15">
                <UniformGrid Columns="2">
                    <StackPanel>
                        <TextBlock Text="DANE FORMY" FontWeight="Bold" Foreground="Gray" FontSize="10"/>
                        <TextBlock Text="{Binding WybranyIndeks.Formy.KodFormy}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="{Binding WybranyIndeks.Formy.Lokalizacja}" FontSize="14" Foreground="DimGray"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="LISTA WSTAWEK" FontWeight="Bold" Foreground="Gray" FontSize="10"/>
                        <TextBlock Text="{Binding TekstWstawek}" FontSize="18" Foreground="#2C3E50" TextWrapping="Wrap" Margin="0,5"/>
                    </StackPanel>
                </UniformGrid>
            </Border>

            <GroupBox Grid.Row="2" Header="Struktura Materiałowa BOM" Margin="0,10">
                <DataGrid ItemsSource="{Binding WidokBOM}" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="200" Background="White" GridLinesVisibility="Horizontal" RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nazwa Materiału" Binding="{Binding Material}" Width="2*" MinWidth="300"/>

                        <DataGridTextColumn Header="Ilość" Binding="{Binding Ilosc, StringFormat=N3}" Width="*" MinWidth="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Left"/>
                                    <Setter Property="Margin" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Jm" Binding="{Binding Jednostka}" Width="60" MinWidth="60">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Udział [%]" Binding="{Binding Udzial, StringFormat={}{0:P1}}" Width="*" MinWidth="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Left"/>
                                    <Setter Property="Margin" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </ScrollViewer>
</UserControl>